<script>
  // PUBLIC_INTERFACE
  /**
   * Runtime configuration for Resume Match Optimizer frontend.
   * Configure your backend base URL here. Do NOT include a trailing slash.
   * Example:
   *   http://localhost:8000
   *   https://dev.api.example.com
   */
  window._CONFIG = window._CONFIG || {
    API_BASE_URL: "http://localhost:8000"
  };
</script>
